{
  "name": "@boundless/db-prisma",
  "version": "0.0.1",
  "private": true,
  "type": "commonjs",
  "main": "./src/index.js",
  "types": "./src/index.d.ts",
  "scripts": {
    "merge:schema": "ts-node scripts/merge-schema.ts",
    "generate": "pnpm run merge:schema && prisma generate --no-engine",
    "migrate": "pnpm run merge:schema && prisma migrate dev"
  },
  "dependencies": {
    "@prisma/client": "^6.12.0",
    "@prisma/extension-accelerate": "^2.0.2",
    "tslib": "^2.3.0"
  },
  "devDependencies": {
    "prisma": "^6.12.0",
    "prisma-nestjs-graphql": "^21.2.0"
  }
}
