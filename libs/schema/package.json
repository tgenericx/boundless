{
  "name": "@boundless/schema",
  "version": "0.0.1",
  "private": true,
  "type": "commonjs",
  "main": "./src/index.js",
  "types": "./src/index.d.ts",
  "scripts": {
    "merge:schema": "ts-node scripts/merge-schema.ts",
    "generate": "pnpm run merge:schema && pnpm dlx prisma generate --no-engine",
    "migrate": "pnpm run merge:schema && prisma migrate dev"
  },
  "dependencies": {
    "prisma": "^6.12.0",
    "prisma-nestjs-graphql": "^21.2.0",
    "tslib": "^2.3.0"
  },
  "devDependencies": {}
}
