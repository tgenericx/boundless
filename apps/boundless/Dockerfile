# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @boundless/boundless`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t boundless-boundless`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system boundless-boundless && \
          adduser --system -G boundless-boundless boundless-boundless

COPY apps/boundless/dist boundless-boundless/
COPY apps/boundless/package.json boundless-boundless/
RUN chown -R boundless-boundless:boundless-boundless .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix boundless-boundless --omit=dev -f install

CMD [ "node", "boundless-boundless" ]
